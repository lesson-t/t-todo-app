<%= link_to board_path(board) do %>
    <div class="card">
        <div class="card_content">
            <div class="card_title"><%= board.title %></div>
            <div class="card_detail">
                <%= image_tag board.user.avatar_image %>
                <div>
                    <p><%= board.author_name %></p>
                    <p><%= board.display_created_at %></p>
                </div>
            </div>
        </div>
    </div>
<% end %>